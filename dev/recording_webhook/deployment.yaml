# 🤖 AUTO-GENERATED - DO NOT EDIT MANUALLY
# Generated by: GitHub Actions
# Service: recording_webhook
# Environment: dev
# Image Version: main-92c9863
# Helm Templates: v1.0.1
# Generated: 2025-11-05 09:22:05 UTC
# Commit SHA: 92c9863094bbbb9430fd67e0dbb37f79c6a59228
# Workflow: https://github.com/ritual-app/recording_webhook/actions/runs/19097167256
---
---
# Source: microservice/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: recording-webhook-microservice
  namespace: dev
  labels:
    helm.sh/chart: microservice-1.0.0
    app.kubernetes.io/name: microservice
    app.kubernetes.io/instance: recording-webhook
    app: recording-webhook
    app.kubernetes.io/version: "1.0"
    app.kubernetes.io/managed-by: Helm
    environment: dev
    app: recording-webhook
    component: webhook
    managed-by: argocd
    team: backend
    tier: api
  annotations:
    prometheus.io/path: /metrics
    prometheus.io/port: "8080"
    prometheus.io/scrape: "true"
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: microservice
      app.kubernetes.io/instance: recording-webhook
      app: recording-webhook
  template:
    metadata:
      labels:
        app.kubernetes.io/name: microservice
        app.kubernetes.io/instance: recording-webhook
        app: recording-webhook
        app: recording-webhook
        component: webhook
        managed-by: argocd
        team: backend
        tier: api
      annotations:
        # Force pod restart on config/secret changes
        checksum/config: 483024cc49b184c20615b42f18e0b703bb2cfeb10a3e3dea65499e9ace7ec82c
        prometheus.io/path: /metrics
        prometheus.io/port: "8080"
        prometheus.io/scrape: "true"
    spec:
      serviceAccountName: recording-webhook-sa
      securityContext:
        capabilities:
          drop:
          - ALL
        fsGroup: 1000
        runAsNonRoot: true
        runAsUser: 1000
      containers:
        - name: microservice
          image: "us-east1-docker.pkg.dev/ritual-app-dev-104fc/docker/recording_webhook:main-92c9863"
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          env:
            - name: ENVIRONMENT
              value: dev
            - name: LOG_LEVEL
              value: DEBUG
            - name: PORT
              value: "8080"
            - name: PROJECT_ID
              value: ritual-app-dev-104fc
            - name: REGION
              value: us-east1
          envFrom:
            - secretRef:
                name: recording-webhook-microservice-secrets
          livenessProbe:
            failureThreshold: 3
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
          readinessProbe:
            failureThreshold: 2
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 3
            periodSeconds: 3
            timeoutSeconds: 2
          startupProbe:
            failureThreshold: 12
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 0
            periodSeconds: 5
            timeoutSeconds: 2
          resources:
            limits:
              cpu: 1000m
              memory: 512Mi
            requests:
              cpu: 100m
              memory: 256Mi
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app
                  operator: In
                  values:
                  - recording-webhook
              topologyKey: kubernetes.io/hostname
            weight: 100
